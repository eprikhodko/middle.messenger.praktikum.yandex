<input
  class="form-input {{#if inputClassName}}{{inputClassName}}{{/if}}"
  type={{type}}
  name={{name}}
  id={{id}}
  pattern="{{pattern}}"
  value="{{value}}"
  placeholder="{{placeholder}}"
  autocomplete="off"
  required
/>
